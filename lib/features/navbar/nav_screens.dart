// booking_schedule_screen.dart

import 'package:flutter/material.dart';

class nav_screens extends StatelessWidget {
  const nav_screens({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Đây là màn hình"), 
      ),
    );
  }
}
